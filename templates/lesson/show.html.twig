{% extends 'base.html.twig' %}

{% block title %}{{lesson.Name}}{% endblock %}

{% block body %}
    <h1>{{lesson.Name}}</h1>
    <a href="{{ path('app_course_show', {'id': lesson.Course.Id}) }}">{{lesson.Course.Name}}</a>
    <table class="table">
        <tbody>
            <tr>
                <th>Content</th>
                <td>{{ lesson.Content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lesson_index') }}">back to list</a>

    <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">edit</a>
    <a href="{{ path('app_lesson_delete', {'id': lesson.id}) }}">delete</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
