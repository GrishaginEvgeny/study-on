{% extends 'base.html.twig' %}

{% block title %}Редактирование урока {{ lesson.Name }} / {{ lesson.Course.Name }} / StudyOn{% endblock %}

{% block body %}
    <h1>Редактирование урока "{{ lesson.SequenceNumber }}. {{ lesson.Name }}" курса "{{ lesson.Course.Name }}"</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Обновить информацию об уроке'}) }}

    <a href="{{ path('app_course_show',{'id': lesson.Course.id}) }}" type="button" class="btn btn-dark my-1">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"></path>
        </svg>
        Вернуться к курсу
    </a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
